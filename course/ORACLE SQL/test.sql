unf:{
    customer_name,
        customer_address,
            customer_phone,
                customer_id,

    vehicle_id,
        vehicle_name,
            vehicle_brand,
                vehicle_type,

    driver_id,
        driver_name,
            driver_phone

    service_id,
        service_name,
            service_type,
                service_duration
                    service_charge,

    veh_CAT_ID,
        veh_CAT_NAME,
            veh_CAT_DESC

    invoic_id
} 